template <typename T> //define T as typename

//use T as return type as well as parameter type
T get_smallest(T num1, T num2) {
  
  return num2 < num1? num2 : num1; //pass output by using conditional operator
  
}
